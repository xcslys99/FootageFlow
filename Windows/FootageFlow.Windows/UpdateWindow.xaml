<Window x:Class="FootageFlow.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FootageFlow" Width="700" Height="620" MinWidth="560" MinHeight="460"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip"
        Background="#FAFBFC" Icon="Assets/FootageFlow.png">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid>
      <Grid.ColumnDefinitions><ColumnDefinition Width="58" /><ColumnDefinition /></Grid.ColumnDefinitions>
      <TextBlock Text="↓" FontSize="38" Foreground="#1677E8" VerticalAlignment="Top" />
      <StackPanel Grid.Column="1">
        <TextBlock x:Name="HeadingText" FontSize="25" FontWeight="Bold" TextWrapping="Wrap" />
        <TextBlock x:Name="VersionText" Foreground="#667085" Margin="0,5,0,0" />
        <TextBlock x:Name="PublishedText" Foreground="#667085" FontSize="11" Margin="0,3,0,0" />
      </StackPanel>
    </Grid>
    <TextBlock x:Name="ReleaseTitleText" Grid.Row="1" FontSize="16" FontWeight="SemiBold"
               TextWrapping="Wrap" Margin="0,18,0,12" />
    <TextBlock x:Name="WhatsNewText" Grid.Row="2" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,8" />
    <TextBox x:Name="NotesText" Grid.Row="3" IsReadOnly="True" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
             AcceptsReturn="True" Padding="12" Background="White" BorderBrush="#D0D5DD" />
    <TextBlock x:Name="SafetyText" Grid.Row="4" Foreground="#667085" FontSize="11"
               TextWrapping="Wrap" Margin="0,12,0,12" />
    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="LaterButton" MinWidth="120" Padding="14,8" Margin="0,0,8,0" Click="Later_Click" />
      <Button x:Name="ViewButton" MinWidth="140" Padding="14,8" Background="#1677E8"
              Foreground="White" FontWeight="SemiBold" Click="View_Click" />
    </StackPanel>
  </Grid>
</Window>
